<div class="col-sm-8">
    <div>
        <h2>{{foundPosts.category}}</h2>
    </div>
    <div>
        {{#each foundPosts.posts}}
            <a href="/post-editor/viewPost/{{this._id}}">
                <h4>{{ this.title }}</h4>
            </a>
        {{/each}}

    </div>

</div>